import type {Progress} from "@/types"; export const emptyProgress:Progress={xp:0,streak:0,completed:[],skills:{listening:0,reading:0,speaking:0,writing:0,vocabulary:0,grammar:0},reviews:[]}; const key="palmivo-progress-v3"; export function loadProgress():Progress{if(typeof window==="undefined")return emptyProgress;try{return{...emptyProgress,...JSON.parse(localStorage.getItem(key)||"{}")}}catch{return emptyProgress}} export function saveProgress(p:Progress){if(typeof window!=="undefined")localStorage.setItem(key,JSON.stringify(p))}